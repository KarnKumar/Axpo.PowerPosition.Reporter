{
  "ReportSettings": {
    // Folder where CSV files will be written.
    // Override via CLI: --ReportSettings:OutputPath "C:\\Reports"
    "OutputPath": "output",

    // How often (in minutes) the extract runs after the first run.
    // Override via CLI: --ReportSettings:IntervalMinutes 30
    "IntervalMinutes": 15
  },

  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Warning",
        "System": "Warning"
      }
    }
  }
}
